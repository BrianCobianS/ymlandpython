all:
  children:
    controllers:
      children:
        mfs:
          hosts:
            olaaaaaaa
      vars:
        ansible_connection: ssh
        ansible_user: user
        ansible_ssh_pass: pasword
        ansible_ssh_extra_args: "-o StrictHostKeyChecking=no -o RequestTTY=yes"